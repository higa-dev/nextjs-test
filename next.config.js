/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  experimental: {
    serverActions: true,
  },
  experimental: {
    // enable alpha server actions
    serverActions: true,
  },
}

module.exports = nextConfig
